# JARVIS Full System Audit — 2026-07-17

Session 69 · read-only audit (no service/config changes made) · all evidence gathered live from the box between 06:19–06:30 UTC.

## Executive summary

The platform is **up** — all 10 core services running, tailnet Gateway (:8443) and Deck (:8444) answering 200, backups green, orchestrator canary passing. But two critical problems are actively burning money and trust:

1. **Self-heal's guardrails are ALL silently disabled** by an env-file parsing bug — it has dispatched **121 live repair agents since 2026-07-13** (117 attempts on vapron *today* against a configured cap of 6), every one a false alarm.
2. **The Anthropic API key is out of credits** — the Deck/Gateway brain has been failing over to the OpenAI fallback (`brain-provider=openai` since 05:54); Craig's interface is not running the claude-fable-5 brain the doctrine promises.

---

## P0 — Critical

### 1. Self-heal guardrails all disabled → runaway agent dispatch
**Severity: CRITICAL · Impact: real Claude spend, alert flood, spurious auto-"repairs" on production boxes**

- `/var/lib/jarvis/self-heal/vapron.json`: `"attemptsToday": 117` (configured cap: 6).
- `/var/log/jarvis-self-heal.log`: `LIVE dispatching repair for vapron (HTTP ???, down 0m, attempt 117)` — dispatch at **0 minutes down** despite a 12-minute debounce, no cooldown honored.
- Dispatch counts by day: Jul 13: 17 · Jul 14: 32 · Jul 15: 28 · Jul 16: 36 · Jul 17 (by 06:20): 8 = **121 live dispatches**.
- Two of yesterday's vapron jobs **failed at the 30-min timeout** (`e458b85c`, `99dccf89`); today's `1f40f05f` completed and confirmed *the site was never down* — the trigger is vapron's own routine post-deploy service restart blip (root-caused repeatedly on the vapron side as "sessions #44–#67").

**Root cause (confirmed):** `config/self-heal.env` uses trailing inline comments:
```
SELF_HEAL_DOWN_MINUTES=12      # site must be down this long before acting (survives a flap)
```
systemd `EnvironmentFile=` does **not** strip trailing comments — the value delivered to node is the string `12      # site must be down…`. `Number(...)` → `NaN`, and every guardrail comparison in `src/self-heal.js` (`downMin < DOWN_MINUTES`, cooldown, daily cap, concurrency cap) evaluates false against NaN → **all four guardrails pass unconditionally**. `SELF_HEAL_MODE=live` has no trailing comment, so live mode works — the worst possible combination.

Secondary bug: the repair prompt reads `HTTP ???` because fleet-check's `notes` don't contain an `HTTP <code>` string the regex expects — agents are dispatched with no actual failure evidence.

**Remediation:**
1. Move every comment in `config/self-heal.env` onto its own line (or drop the comments). Values-only lines. No service restart needed — the timer re-reads per run.
2. Harden `src/self-heal.js`: `const n = Number(parseInt(v,10)); if (!Number.isFinite(n)) use default` for all four knobs, and refuse to run live if any knob is NaN.
3. Consider `SELF_HEAL_MODE=dry-run` until 1–2 land.
4. Address the trigger: fleet-check flags vapron `error` on every deploy restart; either have vapron's deploy agent suppress/announce the blip, or require 2 consecutive failed probes before writing `status=error`.

### 2. Anthropic API credits exhausted — brain running on OpenAI fallback
**Severity: CRITICAL · Impact: the primary interface (Deck/Gateway) is not running the advertised claude-fable-5 brain**

- `journalctl -u jarvis-deck`: repeated `agent brain failed, using intent pipeline: Anthropic API 400 … "Your credit balance is too low to access the Anthropic API"` (04:11–05:54 UTC today).
- Memory KV `brain-provider` = `openai` (set 05:54); `src/lib/agent.js` falls back to `gpt-5.1` via the OpenAI provider path.
- CLAUDE.md's Deck row documents the brain as **claude-fable-5** — currently untrue.

**Remediation:** top up the Anthropic metered key (Plans & Billing), then switch back (voice: "switch the brain to Claude", or `POST :9200/memory/kv {"key":"brain-provider","value":"anthropic"}`). Add a low-credit alert: on the first 400-billing error, fire a `notify()` at alert level (currently it only logs).

---

## P1 — High

### 3. jarvis-audit leaks GateTest test processes — 22 zombies accumulating 2/day
- 22 copies of `/usr/bin/node /opt/gatetest/tests/mcp-http-request.test.js` running, oldest **10.5 days**, each holding a loopback port (the mystery 127.0.0.1:33xxx–46xxx listeners) and RSS.
- Start times (18:00:13 and 18:03:08 daily) match `jarvis-audit`'s gatetest build/test steps exactly (`[audit] gatetest: running build...` 18:00, `running tests...` 18:03).
- `audit-runner.js` uses `execSync` — its timeout cannot reap grandchildren the test script spawns.
**Remediation:** kill the 22 leaked PIDs; in audit-runner spawn tests in their own process group and `kill(-pgid)` on completion/timeout; or fix the gatetest test to exit.

### 4. GateTest audit failing every day — score 30/100, 5 errors, unremediated
- `[audit] gatetest: complete. Score: 30/100 | Errors: 5` on Jul 15 and Jul 16 (identical). A recurring red audit nobody acts on defeats the audit loop's purpose. Also registry says gatetest lives at `/root/gatetest` while CLAUDE.md/session-start use `/opt/gatetest` (roadmap move #8 "fix registry repo mismatches" is still pending).
**Remediation:** dispatch one job to root-cause the 5 errors; reconcile the checkout paths.

### 5. Remote agents run blind — no memory access from fleet boxes
- Today's vapron self-heal agent reported "`the JARVIS memory service at :9200 was offline for both session-start and session-end`" — :9200 is loopback-only on *this* box, so **every remote session starts without context and can never close** (contributes to finding 9).
**Remediation:** serve memory to fleet boxes over the tailnet (e.g. `tailscale serve` on a third port, token-gated) or proxy session-start/end through the orchestrator SSH channel.

### 6. Doctrine drift — CLAUDE.md/ROADMAP.md no longer match the box (Rule 0)
- Registry has **12** platforms (adds `universal-ai-operator`, `marcoreid`, `davenroe`, `screenshot-to-code`); CLAUDE.md lists 8 and explicitly claims MarcoReid is *not* registered.
- `docs/ROADMAP.md` ("THE 20 MOVES", updated 2026-07-08) vs `config/roadmap.json` (23 moves, updated 2026-07-15) — the twins rule is being violated.
- CLAUDE.md says every service has `/health`; five use `/<name>/health` (memory, screenshot, metrics, audit) and deploy-gate uses `/deploy-gate/health` — the "WHEN SOMETHING BREAKS" probe instruction 404s on half the fleet.
- The self-heal controller, fleet-check, jarvis-backup and vapron-backup units/timers exist on the box but are absent from CLAUDE.md's topology.
- CLAUDE.md claims AlecRae binds loopback; `ss` shows 0.0.0.0:4100/:4200 (UFW-restricted to 10.0.1.0/24, so exposure is controlled, but the doc is wrong).
**Remediation:** one doc-sync commit updating CLAUDE.md (services table + registry list + health paths) and regenerating ROADMAP.md from roadmap.json.

### 7. Production code was running untracked in git (mitigated by this session's commit)
- `src/self-heal.js` — live, dispatching agents — plus `config/self-heal.env`, `scripts/pull-vapron-backup.sh`, and ~1,120 lines of uncommitted modifications across 10 tracked files (Deck v2.2, the OpenAI brain fallback in agent.js, registry additions) existed only on this disk. Box loss = code loss, and no other agent could see the real system.
**Remediation:** committed and pushed in this session (see commit message). Keep Rule: ship = commit, same day.

---

## P2 — Medium

### 8. Notification inbox: 262 of 263 unread
Pipeline works (writes, TTS speech, deck push all live) but nothing is consumed — the self-heal flood (finding 1) buried the channel. TTS budget: 1,784/40,000 chars today (57 all day yesterday) — the spam is also speaking. Fix finding 1, then triage/mark-read the backlog; consider auto-collapsing repeats of the same title.

### 9. 15 of 68 sessions never closed (Rule 3)
Was 12/47 at the 2026-07-06 audit — the ratio isn't improving. Partly caused by finding 5 (remote agents *can't* close). Session 68 (05:55 today) also never closed.

### 10. Agent-org scheduler still in dry-run
`AGENTS_MODE=dry-run` since 2026-07-15; 9 social-media role agents log "would dispatch" daily at 18:00–19:20 UTC. Not a defect — awaiting Craig's deliberate flip to `live` (kill switch working as designed). Flagged so it isn't forgotten.

### 11. No build/type-check tooling; `npm run health` is broken
`package.json` has no build/type-check script and no tsconfig exists; `npm run health` → `bash: scripts/health-check.sh: No such file or directory`. Verification in this audit = `node --check` on all 19 src files (all pass) + live health probes. Add a real `check` script (`node --check` loop) and restore or remove the health script.

### 12. Resource posture: acceptable, one watch item
Disk 46% (78G free), inodes 4%, load 0.50/4 cores. RAM 2.7G/7.7G used but **1.3G swap in use** — evidence of past memory pressure (22 leaked processes from finding 3 contribute). Roadmap move #2 (resource guards/pre-OOM alerting) still pending.

### 13. Backups green, but everything still dies with the box
- jarvis.db: WAL-safe backup daily 03:30, 13 snapshots to Jul 17 (latest 499,712 bytes), restore drill previously passed (move #5 done).
- vapron DB: pulled off-box daily 04:17 with gzip + integrity check ("integrity ok", 77.1MB gz today).
- **Gap:** `/opt/jarvis/backups/` lives on the same disk it protects, and nothing off-box monitors Jarvis (known debt #1). An off-box copy of the memory backup is one rsync line inside the existing vapron-backup unit.

### 14. Security posture: good, three watch items
Verified: all Jarvis services loopback-bound; Gateway/Deck tailnet-only (real curl 200 via `--resolve`, per the Gotcha — note the box itself can't resolve MagicDNS, use IP+SNI when probing); UFW default-deny with scoped rules; dashboard token auth on :9206; secrets.env/.ssh gitignored and unstaged; `config/tailscale-acl.json` contains no keys; canary green (CLI 2.1.212 = verified, `canaryHeld:false`).
Watch: (a) workers still run as root with `--dangerously-skip-permissions` (known debt #3 / move #10); (b) Coolify UI on 0.0.0.0:8000 (co-tenant, not ours to change — flag to Craig); (c) a `codex-env.conf` drop-in on jarvis-self-heal sets `CODEX_MODEL=gpt-5.6-sol` — an OpenAI-runtime escape hatch that isn't documented anywhere.

### 15. Minor log findings
- jarvis-memory restarted cleanly at 03:57 today (unexplained; ~2.4h uptime vs 10.9d for its peers) and logged one JSON body-parse stack from a malformed client POST at 05:55 — single occurrence, harmless, but the raw stack suggests no error middleware.
- Orchestrator queue: 71 completed / 2 failed (both = finding 1's timeouts) / 1 canceled (smoke test) / 1 running (this audit). Durable-jobs machinery working as designed.

---

## Priority order for remediation
1. Fix self-heal env parsing + NaN guards (P0.1) — stops the money leak today.
2. Top up Anthropic credits, restore claude-fable-5 brain (P0.2).
3. Kill the 22 leaked processes + fix audit-runner reaping (P1.3).
4. Root-cause gatetest's daily 5-error audit (P1.4).
5. Memory access for remote agents (P1.5) — unblocks session hygiene (P2.9).
6. Doc-sync commit for CLAUDE.md + ROADMAP twins (P1.6).
7. Off-box copy of jarvis.db backups (P2.13) + external watcher (debt #1).
